<UserControl x:Class="GUI_20212202_G1WRGM.Views.ScoreboardControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI_20212202_G1WRGM.Views" 
             xmlns:viewmodels="clr-namespace:GUI_20212202_G1WRGM.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ScoreboardViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="46*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" VerticalContentAlignment="Center" VerticalAlignment="Center">
            <Menu.Background>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="DarkGray" Offset="0.0"/>
                    <GradientStop Color="Orange" Offset="0.5"/>
                </LinearGradientBrush>
            </Menu.Background>

            <MenuItem Header="Select OST" VerticalAlignment="Center" VerticalContentAlignment="Center">
                <MenuItem.Icon>
                    <Image Source="/Assets/Others/SelectOST.png"/>
                </MenuItem.Icon>
                <MenuItem Header="Doom Eternal" Command="{Binding StartDoomEternalOSTCommand}">
                    <MenuItem.Icon>
                        <Image Source="/Assets/Others/RPG.png"/>
                    </MenuItem.Icon>
                    <MenuItem.Background>
                        <ImageBrush ImageSource="/Assets/Levels/Youtube/bunnygirlcpp.jpg"/>
                    </MenuItem.Background>
                </MenuItem>
                <MenuItem Header="Doom 2016" Command="{Binding StartDoom2016OSTCommand}">
                    <MenuItem.Icon>
                        <Image Source="/Assets/Others/shotgun.png"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Needy Streamer Overload" Command="{Binding StartNeedyStreamerOverloadOSTCommand}">
                    <MenuItem.Icon>
                        <Image Source="/Assets/Others/streamer.png"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        
        <!--2. row-->
        <ListBox Grid.Row="1" Margin="20,10,20,10" HorizontalContentAlignment="Stretch">
            <ListBoxItem>
                <Label Content="Kaga1" Background="Orange"/>
            </ListBoxItem>
            <ListBoxItem>
                <Label Content="Kaga2" Background="LightBlue"/>
            </ListBoxItem>
        </ListBox>
        
        <!--3. row-->
        <DockPanel Grid.Row="2">
            <Button Content="Main Menu" Command="{Binding GoToMainMenu}" Width="90" Height="30" />
        </DockPanel>
    </Grid>
</UserControl>
